#!/bin/bash
# ensure docker, python is installed
# source environmental vars from config file
# Create vuls user
# add vuls user to docker group
# Generate ssh keys for VULS_USER
# add /opt/fuzz/bin to $PATH for vuls user
# add env vars to vuls bashrc
# cron setup
# logrotate
